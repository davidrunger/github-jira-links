## Run a dev server and a live-reloading, sandboxed Firefox Developer Edition

```
web-ext run -f /usr/bin/firefox-devedition
```

## Package the extension for permanent use

```
web-ext build --overwrite-dest
```
